# 引き継ぎ文（セッション状態サマリ）

最終更新: 2026-06-25（A-1反証可視化＋U-1フェーズ選択セッション）

## 0. このファイルの目的
セッションが長くなったため状態を要約。次セッションは**まずこれを読んでから**再開すること。

---

## 1. プロジェクト概要
- **「継戦能力版ソブリン格付け」** = フォークランド紛争（アルゼンチン軍事政権）を題材にした「戦争責任監査（War Accountability Audit）」のWebアプリ。vanilla JS 単一ファイル構成。
- 監査原則（最重要・全設計の前提）:
  1. 反証証拠を隠さない
  2. 言えること/言えないことを分ける
  3. **開戦後の証拠を直接証拠にしない**（＝時点性）
- 構成ファイル:
  - `index.html` — サイドバー＋ビュー切替タブ（Overview / Timeline / Pre-War / Assessment / Evidence / Audit Opinion）
  - `app.js`（中心）— `auditData` オブジェクト＋レンダラ群
  - `styles.css`
  - `.claude/launch.json` — プレビュー用静的サーバー定義（`python -m http.server 8123`）

---

## 2. このセッションで完了したこと

> ✅ 直近セッションの成果は順次コミット＋push 済み。

### 2-00. サブエージェントレビュー → A-1 / U-1 適用 ※今セッション
- **サブエージェントレビュー**（UI/シニアエンジニア/監査方法論＋メタ）を再実施。優先度1=A-1、2=U-1 を選択適用（「1+2」指示）。
- **A-1【重大・反証可視化】**: EL-002（E-001 → claim_uk_limited・**反証**）の `assessmentCellId` を `null` → `cell_war_capacity_opening` に。これで Assessment ビューの中心セルが従来「支持5/保留1」のみ＝**反証が見えない**状態だったのを、**支持5/反証1/保留1** に是正。「反証を隠さない」原則が当該ビューでも成立（[app.js] EL-002、意図コメント付き）。
- **U-1【到達不能データの復活】**: Timeline が `phases[0]`（開戦判断）固定で、転換点/終戦判断のリッチデータ（前提・監査の問い・代替案等）が UI から到達不能だった問題を解消。
  - 状態 `activeTimelinePhaseId`（既定 `"opening"`）追加。`timeline-node` 見出しを `<button class="timeline-node-select">` 化（**`<dl>` は兄弟維持＝E-4 の HTML 妥当性を崩さない**）。`aria-pressed`＋キーボード操作対応。クリックハンドラと `.timeline-node.is-selected` スタイル追加。
- 検証: `node --check` OK／Assessment 中心セルに反証 EL-002 表示を確認／3フェーズ選択で終戦判断の監査の問い等が表示・`is-selected`/`aria-pressed` 反映／コンソール警告・エラーなし。
- **未適用のレビュー指摘（次セッション候補）**: U-2（Evidence 行の選択ハイライト無）/ U-3（バッジ配色の含意衝突）/ A-2・S-1（設計判断B不徹底＝ハードコード status・死蔵 `claims[]`）/ A-3（EL-004 の ex-post 混入疑い・要検証）/ S-2（死コード `STATUS_SEVERITY`）/ S-3（"C-/D+" 3箇所ハードコード）/ S-4（ratingBasis 未集計）/ U-4（"不明" 一定行）。優先度は §4 参照。

### 2-0. HANDOFF整合＋#2データ拡充 ※前セッション
- `HANDOFF.md` の「U-4 / E-3 未コミット」記述を、実際の Git 状態（`4202796` で commit/push 済み）に合わせて修正。
- **#2 データ拡充**: E-006 として「HMS Invincible 豪州売却合意（1982年2月25日）」を追加。英国の空母戦力縮小・移転を示す開戦直前の公開シグナルとして扱う。
- **EL-007**: E-006 → `claim_uk_limited` → `cell_war_capacity_opening` を追加。関係は `支持`、時点適合度は `直接`、判断時点で利用可能は `true`。
- ただし、一次発表本文は未収集のため、`collectionState` は「一次発表未収集（二次研究で確認可）」、`reviewState` は「要検証」とした。
- `cell_war_capacity_opening` の opinion と Overview の証拠件数を E-006 追加に合わせて更新。

### 2-0b. U-4 / E-3 完了（証拠一覧の密度緩和＋評価語彙の統一）※前セッション
- **U-4**: Evidence Graph の一覧3列目から `関連度` と `時点適合度` を削除し、一覧の情報密度を下げた。
- `関連度` と `時点適合度` は詳細パネル側には維持。つまり、一覧はスキャン用、詳細は監査確認用という役割分担にした。
- **E-3**: E-003（Franks Report）の `interpretiveReliability` を `低〜中` から `低` に正規化。レンジ値を避け、免責方向バイアスを保守的に低として扱う。
- 検証: `node --check app.js` OK／`git diff --check` OK。

### 2-0c. E-4 完了（dt/dd の dl 配下是正）※前々セッション
- 全パネル横断で `<div class="metric-row"><dt>…</dt><dd>…</dd></div>` が `<dl>` の**外**にあり HTML的に不正だった問題を是正。
- 方式: HTML5仕様で `<dl>` 直下の `<div>` ラップは正当 → 各 metric-row クラスタを **`<dl class="metric-list">`** で囲む（既存構造は不変）。
- 詳細パネル（Evidence）は `<p class="metric-group-label">` がグループ間に挟まるため、**資料／時点／信頼性／リンクの4グループを個別 `<dl>` に分割**（ラベルは `<dl>` の外＝兄弟に維持）。
- CSS に `.metric-list { margin: 0; }` を追加し見た目変化を抑止（[styles.css] 246付近）。
- 検証: `node --check` OK／全6ビューで `<dl>` 外の dt/dd = **0件**／詳細パネルは label→dl 交互で正、`:first-of-type` 余白維持／`.metric-list` computed margin=0px／コンソール警告・エラーなし。

---

> 以下 2-1〜2-7 は前セッション（出典レジストリ整合）の成果。`ffc4e0b` にコミット済み。

### 2-1. EL-003 の関係方向修正（#1-a）
- `claim_uk_limited`（反対仮説「英国反応は限定的」）上で、EL-002（反証＝仮説を弱める）と EL-003 が同じ「反証」で**方向矛盾**していた。
- EL-003 の canSay「英国の即応が困難」は仮説を**強める**方向なので `relationship: 反証 → 支持` に修正（[app.js] EL-003）。意図コメント付き。

### 2-2. 同時代一次史料の追加で ex-ante のゼロ解消（#2 = A＋B）
- **A**: **E-005**（新規）= 1981年英国国防白書 *The Way Forward*, Cmnd 8288（HMS Endurance 撤収方針）を追加。当時公開・入手可能な ex-ante 同時代一次史料。
- **EL-006**（新規）= E-005 → `claim_uk_limited`、`relationship: 支持` / `timeFit: 直接` / `availableAtDecisionTime: true`。これで **EL-006（同時代・支持）⇄ EL-002（事後・反証）** が対になる誠実な状態。
- **B**: `cell_war_capacity_opening` の `opinion` に「楽観的見積もりに一定の酌量余地（E-005／反証を隠さない）」を併記。
  - ⚠️ 当初 criteria に入れかけたが**方向が逆**（criterion は楽観前提を覆す“反対情報”側＝論点を強める。E-005 は酌量＝逆向き）と判明し、criteria は原文維持・opinion 側に正しく併記した。**この区別は再混同しないこと**。

### 2-3. #3 は実体なし（消し込み済み）
- 「E-004 の機密電報→機密文書」は、既に E-004 が「機密解除文書」表記で**修正不要**だった（"電報" は本 HANDOFF にしか存在しなかった）。対応不要として確定。

### 2-4. date の2軸分割（#1の根治）
- 全5 evidence の `date` を **`publishedDate`（刊行/生成年）** と **`coveragePeriod`（記述対象期間）** に分割。
- 表示も詳細パネルで `刊行/生成` + `対象期間` の2行に。`.date` への stale 参照ゼロ。
- 効果: 「戦後刊行だが対象は開戦前」（E-002 ラッテンバッハ / E-003 フランクス）の時点の二重性が明示。

### 2-5. reliability の2軸化（#4）＋ フランクス免責バイアス注記（#5）
- `reliability` → **`authenticity`（文書の真正性）** + **`interpretiveReliability`（解釈の信頼性）**。定義は E-001 直後のコメント参照。
  - 値: E-001 高/中, E-002 高/中, **E-003 高/低（後続修正で正規化）**, E-004 中/中, E-005 高/中, E-006 中/中。
- **#5**: EL-003 の `cannotSay` に「フランクス報告書は結論が英政府を免責する方向のバイアスを含むため、依拠した解釈は割り引く」を追記。E-003 の `interpretiveReliability: 低〜中` と整合。
- 波及修正: 詳細パネル（真正性＋解釈信頼性の2行）、一覧カード/セルメタ（信頼度→真正性）、**フィルタ `reliability` → `authenticity`（真正性）に統一**（state key・select・絞り込みロジック）。`reliability` 参照は grep 0件。

### 2-6. 死蔵 `claims[].status` の削除（レビュー指摘 E-1/E-2 = (P)案）
- `auditData.claims[].status`（重大懸念/弱体化/要検証）は**どこからも render も参照もされない死蔵データ**で、かつ今回の変更でリンク実体と矛盾し得た（claim_uk_limited は支持2/反証1なのに "弱体化" 固定）。
- → `status` を3 claim すべてから削除。claim は `id`/`text`/`type` のみ保持。**設計判断B（status非保存・実行時導出）と一貫**。意図コメント付き。全6ビュー描画でエラーなし＝死蔵だった裏付け。

### 2-7. UI微修正（レビュー指摘 U-1/U-2/U-3 と M-3/U-5）
- **U-1**: 証拠一覧の cell-stack に `解釈信頼性` を追加（バイアス軸を一覧で可視化）。
- **U-2**: 詳細パネルを小見出しでグルーピング。`.metric-group-label` を `styles.css` に追加。
- **U-3**: 詳細パネルの `true/false` → `はい/いいえ`。
- **M-3＋U-5**: `時点適合度(timeFit)` を詳細パネルに常置（従来は一覧のみ）。可用性2フラグ（判断時点で利用可能 / 分析者が知り得た）を「時点」群へ移動。最終グループは **資料 → 時点（刊行/生成・対象期間・時点適合度・可用性2）→ 信頼性（真正性・解釈信頼性）→ リンク（関係・対象・関連度・意思決定者が知っていた・認識根拠）**。時点フィールド定義コメント（timeFit＋可用性2）の区分に一致。

---

## 3. 確定した設計判断（蒸し返さない）
- **A**: regime_survival の override は provisional（前セッション確定）。現状の重大懸念ゼロは正しい。
- **B**: status はデータ非保存・実行時導出（`resolveStatus` / リンク層が単一情報源）。**今セッションで claims.status を削除して B を徹底**。
- **C**: Pre-War マトリクスは derived のみで配置・着色、override は注釈バッジで別レイヤー。
- **校正α**: 重大懸念は「高×形跡なし」のみに限定。現状維持。
- **校正β**: 「評価したが妥当でなかった」軸は追加しない。現状維持。
- **EL-003 = (a)**: `claim_uk_limited` への `支持`（claim 付け替え=(b)案は採らない）。理由: canSay を書き換えず1語修正で整合、フランクスのバイアスを免責証拠根拠にすると二重偏向。
- **#2 = A＋B**: 同時代史料追加＋opinion 酌量併記。criteria への E-005 投入は方向逆のため不採用。
- **E-1/E-2 = (P)**: claims から status を削除（claim集計ビュー新設=(Q)案は凍結）。

---

## 4. 凍結中の項目（メタ視点で逓減と判断。やるなら独立セッション）
- ~~**E-4【最大の残負債】**~~ → **解消済み**（§2-0 参照）。
- ~~**U-4**~~ → **解消済み**（§2-0 参照）。
- ~~**E-3**~~ → **解消済み**（§2-0 参照）。
- **M-1**: claim 単位の支持/反証集計ビュー（死蔵 claims を活かす）。**S-1 と同根**＝`auditData.claims[]` は現状どの renderer も参照しない死蔵。M-1 で活用するか削除するかの去就決定が必要。
- **M-2**: 論理連動する `claim_uk_limited`⇄`claim_uk_response` の横断リンク（既定見送り）。
- **今回レビューの未適用分（優先度順）**:
  - **U-2**: Evidence テーブルに選択中行ハイライト無（Assessment は is-selected あり＝不整合）。
  - **U-3**: バッジ配色の含意衝突（反対仮説への「支持」が緑＝肯定的に見える）。
  - **A-2 / S-1**: 設計判断B不徹底（`assessmentCells[].status`・`overview.issues` 件数・`opinion findings` がハードコードで証拠リンク非連動）。全 derived 化は過剰なので最小整合に留める方針。
  - **A-3**: EL-004/E-004 の ex-post 混入疑い（ヘイグ shuttle は多くが開戦後）。史実は要検証。
  - **S-2**: 死コード `STATUS_SEVERITY`（定義のみ）。即掃除可。
  - **S-3**: 補助格付け "C-/D+" が3箇所（warCase.rating / index.html pill / 文脈）でハードコード。
  - **S-4**: `ratingBasis` の重みが集計に使われない静的表。
  - **U-4**: 詳細パネル「意思決定者が知っていた: 不明」が全リンク一定で情報量ゼロ。
- ~~**#2 データ拡充（第一弾）**~~ → **E-006 / EL-007 追加で一部解消済み**（§2-0 参照）。追加するなら、HMS Invincible 売却合意の一次発表本文、英国議会記録、当時報道の収集が次候補。

> レビュー2巡で**監査軸の新発見はゼロ**＝実質的な監査価値はほぼ枯れた状態。次は機能追加よりコミット/技術負債整理が妥当。

---

## 5. 動かし方 / 検証
- **プレビュー**: `.claude/launch.json` に static サーバー定義（`python -m http.server 8123`）。preview MCP の `preview_start` に `name: "static"`。serverId はセッションごとに変わる。
- **構文チェック**: `node --check app.js`
- **検証方法**: preview の `preview_eval` で `auditData` / `render()` を直接叩いて DOM/ロジック確認。
  - ⚠️ `preview_screenshot` はこの環境で**タイムアウトしがち** → `preview_eval` で `innerText` 構造を取得して証跡にするのが確実。
  - reload は `location.reload()` 後に "Inspected target navigated" エラーが出るが正常（次の eval で再取得できる）。
- 今セッションの全変更でコンソールエラーなし。

---

## 6. 作業スタイル（ユーザーの好み）
- 日本語で応答。
- **「サブエージェントレビュー」** = UIデザイナー / シニアエンジニア / 監査方法論レビュアー（＋メタ視点）の人格をコンテキストなしで立て、粗探し → 優先度表で統合 → 推奨を述べる。確立済みパターン。
- 進め方: 設計を詰める→サブエージェントレビュー→指摘を番号/記号で選択適用（「#1-a」「A+B」「P」「U-1,2,3」のように指示）→プレビュー検証→再レビュー、のループ。
- 過剰設計には**メタ視点で逓減を指摘し凍結を提案**するのが歓迎される（今セッションで M-1/M-2/E-4 等を凍結）。
- 修正後は必ず `node --check` ＋ `preview_eval` で検証してから報告。

---

## 7. 次セッションの推奨アクション
1. まず本ファイルを読む。
2. 作業ツリーは clean（A-1/U-1 まで push 済み）。残る選択肢:
   - **今回レビューの未適用分**（§4 末尾）: 掃除なら `S-2`（死コード即削除）、UX なら `U-2`/`U-3`、方針決めが要るのは `A-2`/`S-1`（設計判断B vs 過剰設計）。
   - **#2 データ拡充の第二弾**（HMS Invincible 売却合意の一次発表本文・英国議会記録・当時報道の収集）。
   - **M-1/M-2**（凍結中の機能追加）を解凍するか、一区切りか。
3. いずれも蒸し返し禁止項目（§3）に抵触しないか確認してから進める。
4. ※ E-4 / U-4(旧) / E-3 は解消済み。今セッションで A-1（反証可視化）・U-1（フェーズ選択）も解消。
